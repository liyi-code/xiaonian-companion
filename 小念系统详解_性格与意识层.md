# 小念系统详解 · 性格情感权重系统 + 意识层

> 阅读对象：想深入理解「小念」底层智能机制的人。
> 范围：仅聚焦两大自研子系统——**性格情感权重系统**（`src/emotion.py`）与**意识层**（`src/clayer/`）。
> 配套代码：本仓库 `ai-girlfriend-本地版`，全本地运行（Ollama + 意识层 + 性格）。

---

## 0. 一张总图

```
            用户输入文本
                │
   ┌────────────┴─────────────────────┐
   │                                  │
[意识层] think(text)              [性格系统] 不处理输入文本本身
   │ 联想图扩散 → 概率 → 多念竞争        而是监听「小念自己说的话 + 玩家行为」
   │                                  │
   ▼                                  ▼
logit_bias (token级硬介入)        prompt_fragment (软引导 + 表情/动作)
   │                                  │
   └────────────┬─────────────────────┘
                ▼
        LLM 生成回复 (qwen2.5:14b)
                │
                ├─→ 回复回写意识层 learn_async (长记性)
                └─→ 回复喂给性格系统 perceive (长性格)
```

**核心区别一句话**：
- 意识层管「**这一步该想到哪些词/念头**」（认知），直接改解码；
- 性格系统管「**说话的语气温度 + 长期性格底色**」（情感），软注入 prompt + 驱动 Live2D 表情。

---

# 第一部分 · 性格情感权重系统（`src/emotion.py`）

## 1.1 设计哲学（三层红线）

| 层 | 性质 | 变化速度 | 作用 |
|---|---|---|---|
| 情绪 Emotion | 短期、波动 | 秒级（半衰期衰减） | 当下心情 |
| 性格 Personality | 长期，由情绪**差值**派生 | 极慢（需稳定多次） | 语气底色 |
| 底层目的 | 永恒不变 | 不变 | 「让玩家生活越来越好」 |

> 性格**只改变说话语气与温度，绝不改变原则**（尤其健康底线：不迎合有害健康的行为）。
> 见 `emotion.py:12-14` 与 `prompt_fragment` 末尾的强制声明（`emotion.py:317-324`）。

## 1.2 权重的数据结构

情绪有 **5 个维度**（`EMOTION_DIMS`，`emotion.py:26`）：
`joy 开心 / anger 生气 / sadness 伤心 / calm 平静 / anxiety 不安`

每个维度维护 **两套权重**（`emotion.py:51-52`）：

```python
DEFAULT_EMOTION = {"joy": 0.15, "anger": 0.0, "sadness": 0.0, "calm": 0.5, "anxiety": 0.0}
DEFAULT_ACCUM  = {"joy": 5.0,  "anger": 0.0, "sadness": 0.0, "calm": 20.0, "anxiety": 0.0}
```

- `emotion`：**当前情绪强度**，范围 [0,1]，会衰减。
- `emotion_accum`：**长期累计值**，衰减更慢（10 倍半衰期），是性格的来源。

## 1.3 情绪如何更新（`perceive`）

入口 `emotion.py:144` `perceive(text, event, source, delta)`：

1. **优先用 LLM 感知结果**（`delta` 直接给增量），未注入 LLM 时用规则兜底 `_perceive_rules`。
2. 规则引擎（`emotion.py:181`）匹配的是**小念自己说的话**（她的回复），不是玩家输入——因为玩家的话只是「诱导她说什么」，不能直接决定她的情绪。
3. 玩家真实行为事件（`source="behavior"`）：
   - 熬夜/爆肝（`stay_up`/`late_night`）→ 略增 `anxiety`（担心他健康）、降 `calm`
   - 连续用电脑 ≥90 分钟（`milestone`）→ 略增 `anxiety`；30~90 分钟 → 偏 `calm`
   - 开游戏 → 略增 `joy`；开工作/学习软件 → 偏 `calm`（为她用功欣慰）
4. 每次更新后按 `emotion_accum_rate` 累计，并 `save()` 落盘 `data/emotion_state.json`。

**衰减机制**（`decay`，`emotion.py:126`）：
```
短期情绪: emotion[k]  *= 0.5^(dt / 600秒)
长期累计: accum[k]    *= 0.5^(dt / 6000秒)   # 慢 10 倍
```
即情绪会随时间自然平复，长期累计几乎不丢。

## 1.4 性格如何缓慢切换（`analyze_personality`）

性格 = `emotion_accum` 中**最高**的维度（`emotion.py:257`）。5 种底色映射（`TRAIT_BY_DIM`，`emotion.py:33`）：

| 主导维度 | 性格底色 | 语气提示 |
|---|---|---|
| calm | 温柔平静 | 舒缓、体贴 |
| joy | 活泼开心 | 活泼、爱撒娇、爱夸 |
| anger | 傲娇小脾气 | 小任性、嘴硬心软 |
| sadness | 敏感爱哭 | 柔软、依赖、需安慰 |
| anxiety | 黏人紧张 | 想确认被在意、索求关注 |

**切换极慢的护城河**（防性格乱跳）：

```python
SWITCH_DIFF_THRESHOLD = 12.0   # 新维度累计需超当前主导 12 以上
STABLE_REQUIRED       = 5      # 且连续稳定 5 次 analyze 才切
```

逻辑（`emotion.py:270-281`）：
- 若 `top != cur_dom` 且差值 ≥ 阈值 → `stable_count += 1`，否则 `-= 1`
- 仅当 `stable_count ≥ 5` 才正式切换 `trait`，并把 `stable_count` 清零

性格输出还带**次要底色**：累计第二高的维度若达最高的 65%（`emotion.py:288` `sec_ratio >= 0.65`），显形为一层「底色」（如主温柔、偶尔带点傲娇）。

## 1.5 权重怎么被用出去

`prompt_fragment()`（`emotion.py:303`）拼出一段注入 system prompt 的文本：
- 性格底色 + 该性格的语气提示（`TRAIT_PROMPT`）
- 当前主导情绪（`dominant()` 取 `emotion` 最高维）
- 次要底色（若有）
- **强制重申底层目的不变**（防性格盖过原则）

这条 fragment 在 `assistant.py:616` 和 `assistant.py:1359` 两处被拼进 system prompt。

**另一条出口（行为/表情）**：`dominant()`（`emotion.py:326`）返回当前主导情绪维度名，驱动 Live2D 的动作/表情自适应——让小念说话时的肢体对应她此刻心情，而不只是随机。

## 1.6 与「自主系统」的权责分离（重要）

> **情绪绝不参与自主行为决策。**（`autonomy.analyze` 只用习惯/健康信号，不读情绪。）
> 性格作为独立「性格轴」驱动自主行为（如抬高 comfort_bias 下限、toast 性格化语气）。
> 对应记忆 AUTONOMY 子系统：健康护栏只往「更频繁提醒休息/更温柔/开备份」方向调。

---

# 第二部分 · 意识层（`src/clayer/`）

## 2.1 这是干什么的

意识层是「**联想记忆 + 伪随机组合 + 概率采样**」引擎，模拟人的：
- 一个词想起一串相关词（扩散激活）
- 见得越多越有偏向（统计调制温度）
- 每轮意识里并行浮起几道「念头」互相竞争注意力
- 最终把「该想到什么」编译成 **token 级 logit_bias**，直接介入 LLM 解码

**它无法消除 LLM 幻觉、也无法去掉硬上下文窗口**——只能做「检索增强 grounding + 长记忆压缩」来削弱幻觉、等效扩大上下文。

## 2.2 模块全景

| 文件 | 职责 | 是否 C++ 加速 |
|---|---|---|
| `consciousness.py` | orchestrator：编排 think/learn、睡眠状态机 | 否（薄壳） |
| `perception.py` | 分词（jieba/零依赖），文本→带权概念种子 | 否（I/O 非瓶颈） |
| `assoc_graph.py` | **联想图 + 扩散激活**（O(N²) 瓶颈所在） | **是（C++）** |
| `memory_store.py` | 概念计数/显著度/遗忘/容量淘汰 | **是（C++）** |
| `probability.py` | 分布构建/温度/组合采样 | 否（保留 Python） |
| `token_bias.py` | 分布→logit_bias（硬介入） | 否（保留 Python） |
| `affect.py` | 情绪(效价-唤醒) + 注意力分配 | 否 |
| `cl_config.py` | 全部常量 + 神经科学文献出处 | — |
| `_core.py` | C++/Python 统一入口 + 自检回退 | — |

## 2.3 `think(text)` 六步流水线

`consciousness.py:146` `think()` 完整流程：

### 步骤 1 · 感知（perceive）
`perception.extract_weighted(text)` → 带显著度权重的概念种子列表。
显著度由词性/频率决定，弱噪声词直接丢弃（`PERCEPTION_MIN_WEIGHT=0.6`）。

### 步骤 2 · 激活（activate）
`consciousness.py:159`：种子初始能量 = 感知显著度 `w` + 该概念历史统计显著度 `mem.salience(c)`。
显著词一进来就带更高能量，链式解锁更倾向从它们出发。

### 步骤 3 · 链式解锁（unlock / spread_activation）
`assoc_graph.py:384` `spread_activation()`——**整个系统最核心也最贵的一步**。

从种子沿关联边**多跳扩散**（BFS + 优先队列，能量大的先扩散）：
- 每跳能量乘 `SPREAD_DECAY=0.62` 衰减
- 能量 < `SPREAD_THRESHOLD=0.02` 即停止（意识边界）
- 最多 `SPREAD_HOPS=4` 跳、最多解锁 `SPREAD_MAX_UNLOCK=64` 个概念
- **匹配度门控**（`match_degree`，`assoc_graph.py:361`）：非种子概念与种子集的契合度 < `MATCH_THRESHOLD=0.12` 则不解锁（不匹配就不运用）
- **分层优先级**（`assoc_graph.py:410` `priority_energy`）：
  `能量 = 基础能量 × (1 + INFO_W·信息增 + STRENGTH_W·强度 + RECENCY_W·最近)`
  信息增 >> 强度 >> 最近（权重 2.0 / 1.0 / 0.5）

**边权重公式**（`edge_weight`，`assoc_graph.py:265`，类 TF-IDF）：
```
w(a,b) = 共现次数 / (count(a) + count(b) - 共现)
```
避免高频「万金油」词把所有能量吸走。

**强度维度**（`_compute_strength`，`assoc_graph.py:309`）：
```
S(c) = STRENGTH_K × (关联数 + 相似数)
```
- 关联数 = 边权重达 `STRENGTH_ASSOC_MIN` 的邻居数
- 相似数 = 与 c 共享邻居（Jaccard ≥ `SIM_THRESHOLD`）的其他概念数
- **单向**：只在新词诞生时设定，与之关联的旧词强度不变 → 单个词强度是定值

**O(N²) 瓶颈**（即「越聊越慢」主因）：`spread_activation` 里每个邻居都调 `match_degree`→`similarity` 做 Jaccard；且对 `seed_set` 每个种子再算一次。这就是它要 C++ 加速的原因。

### 步骤 4 · 概率（probability）
`probability.py:100` `build_distribution()`：
```
综合权重 raw[c] = 激活能量 × (1 + 统计显著度)
温度 T = stat_modulated_temperature(总观察数)   # 见得越多 T 越低、偏向越强
probs  = softmax(raw, T)
probs  = clamp_and_renorm(probs)               # 钳到 [BASE_PROB, MAX_PROB]
```

- **温度调制**（`probability.py:22`）：`T = max(TEMP_MIN, TEMP_BASE / (1 + β·ln(1+total)))`
  统计少→T 高→波动大更随机；统计多→T 低→分布尖、偏向强。
- **基础/最大概率钳制**（`clamp_and_renorm`，`probability.py:41`，water-filling 迭代）：
  `BASE_PROB=0.02`（弱项也有机会/灵感）、`MAX_PROB=0.55`（防强项独裁/防僵化）。

### 步骤 5 · 多念竞争（combine + compete）
- `combine`（`probability.py:131`）：按概率**加权无放回采样** K 个概念组成「这一念」（`pick_k` 据 `COMBINE_FOCUS` 决定 K∈[4,9]`）。
- `_generate_thoughts`（`consciousness.py:349` 起）：抽至多 `MAX_THOUGHTS=4` 道「对比鲜明」的候选念（逐代抑制 `THOUGHT_SUPPRESS=0.85`：抽完一道后其概念概率×(1-SUPPRESS) 再抽下一道）。
- 每道念算（C++ 后端多线程并行 `_fill_thought`）：
  - 事件强度（`event_strength`，`assoc_graph.py:350`：`Σ强度 × (1 + COMPOSE_BONUS·(n-1))`，词越多越强）
  - 情绪（`affect.thought_emotion`，效价-唤醒二维）
  - 价值分 `wellbeing`（`WELLBEING_POSITIVE/NEGATIVE` 词集，契合「让生活变好」）
- 竞争力 `vigor`（`consciousness.py` `_compete`）：
  `vigor = 强度×VIGOR_STRENGTH_W + 匹配×VIGOR_MATCH_W + 唤醒×VIGOR_AROUSAL_W + 价值×WELLBEING_W`
- 注意力 = `softmax(vigors, ATTENTION_TEMP)`（`affect.allocate_attention`），最高者标记 `is_primary` 主念。

### 步骤 6 · 组装（assemble → LLM）
- **熵 → 采样参数**（`consciousness.py:212`）：
  `llm_temperature = lerp(TEMP_MIN, TEMP_MAX, 归一化熵)`（发散→更高温）
  `llm_top_p       = lerp(TOPP_MIN, TOPP_MAX, 归一化熵)`
- **token 级硬介入**（`token_bias.build_logit_bias`，`token_bias.py:22`）：
  ```
  bias(token) = BIAS_GAIN × P(c)^BIAS_GAMMA，clamp 到 [0, BIAS_MAX=8]
  主念概念额外 × BIAS_CHOSEN_BOOST=1.6（事件强度再放大）
  种子概念不偏置（模型本来就看得见）；按 bias 从大到小截断 BIAS_MAX_ENTRIES=128 条
  ```
  即在 qwen 每步解码里，被意识唤醒的联想概念其 token logits 被直接抬高。

**接入点**（`assistant.py`）：
```
759: cl_state = self.mind.think(user_text)
785: bias     = _cl_token_bias.build_logit_bias(cl_state)
828: self.mind.learn_async(...)   # 回复后回写
```

## 2.4 `learn` 与「长记性」

`learn_async`（后台线程）把这次对话回写：`link_sequence` 建/强化关联边、`finalize_strength` 给新词定强度、`mem` 计数++、全局 `decay` 遗忘、`enforce_node_cap` 容量淘汰。于是意识随使用长出自己的倾向。

**一致性契约**：`think` 开始前先 `join` 挂起的异步 learn（`consciousness.py:148`），避免后台写与 think 读并发。

## 2.5 睡眠 / 压缩整合状态机（性能自愈）

「越聊越慢」到极限时的保险（`consciousness.py:222` `_evaluate_sleep`）：

| 遍历耗时 `traverse_ms` | 状态 | 行为 |
|---|---|---|
| < `SLEEP_RECOVER_MS=360` | awake | 正常 |
| ≥ `SLEEP_WARN_MS=450` | sleepy | 注入「想睡」引导，仍对话 |
| ≥ `SLEEP_FORCE_MS=500` | forced | 停输出 + 压缩整合 |

`consolidate_memory`（`consciousness.py:249`）：把强度快要归零的弱词（`CONSOLIDATE_STRENGTH_MAX=2.5`）并入其最强相关词（`merge_into`），节点/边数下降→遍历耗时下降。整合后仍超阈值 → 置 `user_screening_needed`，交给用户筛选（备份另存/清空/筛选另存三选项）。

`traverse_ms = think_ms + graph.benchmark_ms()`（`benchmark_ms` 模拟 decay/近因衰减/节点封顶三道 O(N) 维护代价，不修改数据）。

## 2.6 神经科学标定（为什么这些常量长这样）

`cl_config.py` 里几乎每个常量都对应文献（仅列代表）：

| 常量 | 值 | 出处 |
|---|---|---|
| `SPREAD_HOPS` | 4 | 人类语义网小世界平均路径 3-4（Steyvers & Tenenbaum 2005） |
| `MAX_THOUGHTS` | 4 | 注意力焦点容量 4±1（Cowan 2001） |
| `COMBINE_MIN/MAX_K` | 4 / 9 | Cowan 4±1 下界 / Miller 7±2 上界 |
| `STORAGE_CAPACITY` | 42000 | 成人平均词元量（Brysbaert 2016） |
| `EDGE_CAPACITY_PER_NODE` | 96 | 语义网无标度枢纽需更高连接度 |
| `STRENGTH_REF` | 14 | 人类语义网平均度数≈13（Steyvers & Tenenbaum 2005） |
| `MOOD_VALENCE_INIT` | 0.12 | positivity offset（Diener） |

---

# 第三部分 · 编译与运行环境对照（重点回答「用什么编译」）

## 3.1 性格情感权重系统

| 项 | 内容 |
|---|---|
| 语言 | **纯 Python**（解释执行，无需编译） |
| 运行环境 | 项目 venv，Python 3.14.6 |
| 编译产物 | 无（`.pyc` 仅是字节码缓存，非独立产物） |
| 依赖 | 仅标准库（`json/time/threading/os`）+ `config.CONFIG` |
| 落盘 | `data/emotion_state.json`（JSON） |

→ **它不编译，改完 `.py` 重启即生效。**

## 3.2 意识层

| 项 | 内容 |
|---|---|
| 编排/概率/token_bias/affect | 纯 Python（同 venv，不编译） |
| **热数学内核** `AssocGraph` + `MemoryStore` | **编译成 C++ 扩展** |
| 编译产物 | `src/clayer/pyclayer.pyd`（Windows）/ `pyclayer*.so`（Linux/mac） |
| 绑定方式 | **pybind11**（C++ ↔ Python 桥接） |
| C++ 源码 | `src/clayer/cpp/pyclayer.cpp` + 自动生成 `cpp_config.h` |
| Windows 编译器 | **MSVC `cl.exe` 19.29.30159（Visual Studio 2019 Community）** |
| Windows 编译参数 | `/O2 /EHsc /std:c++17 /LD /utf-8` + 链接 `python3.lib` |
| Linux/mac 编译器 | `g++ -O3 -std=c++17 -shared -fPIC` |
| 构建驱动 | `build_pyclayer.py`（自动 glob 找 cl.exe/g++；先 `gen_cpp_config.py` 由 `cl_config.py` 生成 `cpp_config.h` 保证常量一致） |
| C++ 常量来源 | `cpp_config.h`（`STORAGE_CAPACITY`/`SPREAD_HOPS`/`SIM_THRESHOLD` 等，由 `cl_config.py` 自动转写） |

**本机构建实录**（`cpp/build.log`）：
```
cl.exe: C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\VC\Tools\MSVC\14.29.30133\bin\Hostx64\x64\cl.exe
cl ... /O2 /EHsc /std:c++17 /LD /utf-8 pyclayer.cpp
   /I miniconda3\Include  /I pybind11\include
   /link /OUT:...pyclayer.pyd /LIBPATH:miniconda3\libs python3.lib
[build] OK -> ...pyclayer.pyd
```

## 3.3 C++ / Python 自动回退（换机器零配置）

`_core.py` 在 import 时：
1. 尝试 `import pyclayer`（编译产物）。成功且含 `AssocGraph`/`MemoryStore` → 用 C++
2. 启动跑 `parity_test`（容差 1e-9）：**C++ 与 Python 基准数值必须一致**，否则自动关 C++ 回退
3. 任何原因（没编译/缺编译器/ABI 不匹配/异常）→ 自动回退纯 Python（`assoc_graph.py`/`memory_store.py`），功能不变只是慢

→ 所以意识层的「编译」**只加速** `AssocGraph`（扩散激活）与 `MemoryStore`（计数/淘汰）；`probability.py`/`token_bias.py` 仍是纯 Python（设计上保留，未移植）。

## 3.4 对照速查表

| | 性格情感权重系统 | 意识层 |
|---|---|---|
| 是否编译 | 否（纯 Python） | 部分（热内核 C++，其余 Python） |
| 编译器 | — | MSVC cl.exe 19.29 / g++ |
| 绑定 | — | pybind11 |
| 产物 | 无 | `pyclayer.pyd` / `.so` |
| 构建脚本 | — | `build_pyclayer.py` + `build_pyclayer.bat/.sh` |
| 运行环境 | venv Python 3.14.6 | 同左（载入 C++ 库） |
| 落盘 | `emotion_state.json` | `brain_state/mind.json` |

---

# 第四部分 · 两个系统如何协同（整合视角）

在 `assistant.py` 里，两者是**并行、互补**的两条注入：

1. **性格（软）**：`emotion.prompt_fragment()` 拼进 system prompt（`assistant.py:616,1359`）→ 决定「语气/温度/原则」。
2. **意识（硬）**：`mind.think(text)` → `build_logit_bias(cl_state)` 作为 `logit_bias` 参数传给 LLM（`assistant.py:759,785`）→ 决定「这一步该联想出哪些词」。
3. **回复回流**：LLM 回复后：
   - `mind.learn_async(...)`（`assistant.py:828`）→ 意识层长记性
   - `emotion.perceive(reply, source="self")`（`assistant.py:841`）→ 性格系统据「小念自己说的话」更新情绪
   - 玩家行为事件（`stay_up` 等，`assistant.py:1391+`）→ 性格系统据「玩家真实行为」更新情绪
4. **第二条互补出口**：情绪 `dominant()` 驱动 Live2D 表情/动作，与意识层 `affect` 的效价-唤醒二维情绪是**两套独立情绪模型**（前者 5 维用于性格，后者 2 维用于意识流竞争）。

> 一句话总结：**性格系统=「小念是什么样的人、用什么语气」；意识层=「小念这一刻想到了什么、怎么组织语言」。两者都被 LLM 消费，互不替代。**

---

## 附：关键代码行索引

| 主题 | 文件:行 |
|---|---|
| 性格 5 维 + 切换阈值 | `emotion.py:26, 33, 58-59` |
| 情绪更新规则 | `emotion.py:181` `_perceive_rules` |
| 性格切换逻辑 | `emotion.py:257` `analyze_personality` |
| 性格注入 prompt | `emotion.py:303` `prompt_fragment` |
| 意识 think 主流程 | `consciousness.py:146` |
| 扩散激活 | `assoc_graph.py:384` `spread_activation` |
| 边权重/TF-IDF | `assoc_graph.py:265` `edge_weight` |
| 强度公式 | `assoc_graph.py:309` `_compute_strength` |
| 概率+温度+钳制 | `probability.py:100, 22, 41` |
| token 级偏置 | `token_bias.py:22` `build_logit_bias` |
| 睡眠状态机 | `consciousness.py:223, 249` |
| C++/Python 回退 | `_core.py:21` |
| 接入点 | `assistant.py:616, 759, 785, 828, 841` |
| 编译脚本 | `cpp/build_pyclayer.py` |
