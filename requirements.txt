openai>=1.30.0
python-dotenv>=1.0.0
psutil>=5.9.0
websocket-client>=1.8.0
pywebview>=5.0
numpy>=1.24.0
sounddevice>=0.4.6
requests>=2.31.0
faster-whisper>=1.0.0
Pillow>=10.0.0
websockets>=12.0
python-osc>=1.8.3

# 声纹识别（自建 3D 语音通道）：wespeaker 依赖 torch，项目 venv(Python 3.14) 无 torch 轮子，
# 改由带 torch 的环境跑子进程（src/speaker_worker.py）。安装：
#   D:\sovits_env\python.exe -m pip install wespeaker
